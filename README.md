# TSN-Libraries

## Needed to push

For pushing into the repo make sure you have a .gitignore file with at least the following content:

```
# To ignore
*

# Not to ignore
!*.c
!*.h
!*.md
!makefile
```